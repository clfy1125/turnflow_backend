# ============================================================
# Turnflow Backend — Production Environment Variables
# 서버에서 .env.production 으로 복사 후 실제 값을 채워주세요.
# ============================================================

# Django Settings
SECRET_KEY=CHANGE_ME_TO_A_LONG_RANDOM_STRING
DEBUG=False
DJANGO_SETTINGS_MODULE=config.settings.prod

# Database (PostgreSQL via PgBouncer — 하드닝 후)
DB_NAME=instagram_service
DB_USER=postgres
DB_PASSWORD=CHANGE_ME_STRONG_DB_PASSWORD
# 앱은 PgBouncer(트랜잭션 풀)를 통해 접속. 마이그레이션 one-shot 만 db:5432 직결(deploy.sh).
DB_HOST=pgbouncer
DB_PORT=6432
# PgBouncer transaction pooling 필수 설정:
DB_CONN_MAX_AGE=0
DB_DISABLE_SERVER_SIDE_CURSORS=True
DB_CONN_HEALTH_CHECKS=False

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Hosts & Security
ALLOWED_HOSTS=api.turnflow.clfy.ai.kr
SECURE_SSL_REDIRECT=False

# CORS
CORS_ALLOWED_ORIGINS=https://link.turnflow.clfy.ai.kr
CORS_ALLOW_CREDENTIALS=True
CORS_ALLOW_ALL_ORIGINS=False

# CSRF
CSRF_TRUSTED_ORIGINS=https://api.turnflow.clfy.ai.kr,https://link.turnflow.clfy.ai.kr

# Celery
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# Logging
LOG_LEVEL=INFO

# Instagram API
INSTAGRAM_APP_ID=YOUR_INSTAGRAM_APP_ID
INSTAGRAM_APP_SECRET=YOUR_INSTAGRAM_APP_SECRET
INSTAGRAM_REDIRECT_URI=https://api.turnflow.clfy.ai.kr/api/v1/integrations/instagram/connect/callback/
INSTAGRAM_MOCK_MODE=False
INSTAGRAM_WEBHOOK_VERIFY_TOKEN=YOUR_WEBHOOK_VERIFY_TOKEN

# Meta App (Facebook Login for Instagram Business)
META_APP_ID=YOUR_META_APP_ID
META_APP_SECRET=YOUR_META_APP_SECRET

# Google OAuth
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID

# OpenAI (optional)
OPENAI_API_KEY=YOUR_OPENAI_API_KEY

# ── 하드닝(P2/P3f) 토글 ──
# 웹훅 echo/read 비동기 멱등 처리 (문제 시 False 로 즉시 롤백, 재배포 불필요)
WEBHOOK_ASYNC_MESSAGING=True
# 계정당 발송 안전속도 거버너 — 플랜별 (시간당,분당) 한도. JSON 으로 오버라이드 가능(미설정 시 코드 기본값).
# DM_RATE_LIMITS 는 settings 에서 dict 로 관리 (rate_governor.py 참고).
