# [백엔드 회신] DM 캠페인 게시물 썸네일 — 서버가 사본을 보관하는 방식으로 해결했습니다

작성: 2026-08-05 (백엔드) · 대상 문서: `backend-dm-thumbnail.md` (프론트, 2026-08-05)
관련 화면: DM 자동화 캠페인 **목록 카드** · **상세 왼쪽 요약**

---

## 0. 세 줄 요약

1. **진단이 한 가지 어긋나 있었습니다.** 200자 한도 때문이 아닙니다. 운영 캠페인 77건의
   `media_url` 최대 길이는 **72자**로, 잘린 적이 한 번도 없습니다. 진짜 원인은
   **`media_url` 에 2026-08-03 부터 게시물 permalink(HTML 페이지 URL)가 채워지기 시작했고,
   `thumbnail_url` 이 그 값의 미러였다**는 것입니다 → `<img src="https://www.instagram.com/p/…">`
   → 전부 깨짐. **운영 77건 중 68건(88%)이 이 상태**였습니다.
2. 그래서 컬럼을 넓히거나 CDN URL 을 실어 보내는 대신, **게시물 이미지를 우리 스토리지에
   한 번 내려받아 사본을 보관**하고 그 영구 URL 을 `thumbnail_url` 로 줍니다.
   **만료되지 않고, 게시물이 삭제된 뒤에도 남습니다.** (IG 프로필 사진에 이미 쓰고 있는 방식)
3. 요청하신 5건 **전부 반영**했습니다. 상세 응답에 `thumbnail_url`(+통계 필드) 추가,
   `media_ids` 다건 조회 추가, 자리표시자 행 정리, 필드 정책 확정, 엔드포인트 문서화.

---

## 1. 왜 "저장" 도 "조회 시점 보강" 도 아닌 "사본 보관" 인가

프론트 문서 5번의 지적이 정확했습니다 — **컬럼을 넓혀도 근본 해결이 아닙니다.**
IG CDN URL 은 서명된 일시 URL(`?_nc_ohc=…&oe=…&oh=…`)이라 **길이가 아니라 만료**가 문제입니다.
선택지를 이렇게 놓고 봤습니다.

| 방식 | 만료 | 게시물 삭제 후 | 응답 비용 |
|---|---|---|---|
| ① CDN URL 을 DB 에 저장 | ❌ 수시간~수일 뒤 깨짐 | ❌ | 0 |
| ② 매 조회 때 Graph 재조회 후 응답에만 실음 | ✅ | ❌ | ❌ 항목당 외부 HTTP 1회(순차) |
| **③ 이미지를 우리 스토리지에 복사** | ✅ **영구** | ✅ **남는다** | ✅ 0 (DB 컬럼) |

③ 을 택했습니다. 스토리 답장 캠페인처럼 **원본이 24시간 뒤 사라지는** 경우에도 썸네일이
남는다는 점이 특히 큽니다. 저장 경로는 프로필 사진과 같은 규약
(`ig_thumbnails/{ig_user_id}/{콘텐츠해시}.jpg`, 640px 로 축소, 같은 이미지는 파일 1개 공유)입니다.

> 참고로 ② 는 실제로 목록 응답에 구현돼 있었습니다(항목당 타임아웃 5초 순차 호출).
> 이번에 **제거**했습니다 — 느린 데다, 릴스에서는 `.mp4` 를, 캐러셀에서는 아무것도
> 얻지 못하는 결함이 있었습니다(아래 3절).

---

## 2. [요청 1·2 해결] 상세·목록 모두 `thumbnail_url` — 규칙 동일

```
GET /api/v1/integrations/auto-dm-campaigns/{id}/     ← thumbnail_url 추가 (기존엔 키 자체가 없었음)
GET /api/v1/integrations/auto-dm-campaigns/          ← 기존과 같은 키, 의미가 바뀜(=이미지)
```

| 필드 | 타입 | 의미 |
|---|---|---|
| `thumbnail_url` | `string \| null` | **게시물 썸네일 이미지**. 우리 도메인(`https://media.turnflow.clfy.ai.kr/ig_thumbnails/…`)의 영구 URL. `<img src>` 에 그대로 쓰세요. |
| `media_url` | `string \| null` | **게시물 permalink**(`https://www.instagram.com/p/…`). "게시물 보기" 링크용. **이미지가 아닙니다.** |

- **상세 응답에 통계 필드도 함께 넣었습니다**(선택 요청분): `delivered_count`,
  `delivery_rate`, `needs_attention_count`, `last_sent_at`. 상세용 stats 호출이 필요 없습니다.
- `thumbnail_url` 은 **모델 컬럼**이라 추가 쿼리·추가 외부 호출이 0 입니다.

### `null` 이 나오는 경우와 프론트 처리

| 상황 | 값 | 프론트 |
|---|---|---|
| 캠페인 생성 직후 몇 초 (워커가 IG 에서 받아오는 중) | `null` | 플레이스홀더 → 잠시 뒤 재조회하면 채워집니다 |
| `any_media` / 게시물 미지정 캠페인 | `null` | 게시물 개념이 없으므로 정상 |
| 게시물 삭제 / 토큰 만료로 끝내 못 받음 | `null` | 플레이스홀더 유지 |

> 목록·상세를 조회하면 서버가 **비어 있는 항목의 확보를 스스로 예약**합니다(응답은 막지 않음).
> 즉 화면을 한 번 열었다가 다시 열면 채워져 있습니다. 6시간 주기 스위퍼가 최종 안전망입니다.

### 프론트에서 걷어내도 되는 것

| 임시 대응 | 이제 |
|---|---|
| 상세: `media_id` 로 커서 페이지네이션 50개×4쪽 조회 | **제거 가능** — `thumbnail_url` 사용 |
| 목록: 빈 카드 모아 미디어 맵 1회 조회 | **제거 가능** |
| 만료 URL 실패 후 계속 숨던 렌더 버그 수정 | **유지 권장** (일반 방어) |
| Unsplash 자리표시자 저장·표시 차단 | **유지** (서버도 저장 차단하지만 이중 방어) |

`thumbnail_url` 은 **키가 항상 존재**하고 값만 `null` 일 수 있습니다. 하위호환 로직은
그대로 둬도 무해하니, 서버 배포 확인 후 순차적으로 걷어내셔도 됩니다.

---

## 3. 덤으로 고친 것 — 릴스·캐러셀 썸네일

기존 보강 로직은 `media_url` 하나만 봤습니다. Graph 응답은 종류별로 이미지가 실리는 필드가 다릅니다.

| 게시물 종류 | 예전 결과 | 지금 |
|---|---|---|
| 이미지 | `media_url` (정상) | 동일 |
| **릴스·동영상** | `media_url` = **`.mp4`** → `<img>` 에서 깨짐 | `thumbnail_url`(커버 이미지) |
| **캐러셀** | 부모에 `media_url` 이 없어 **아무것도 못 얻음** | **첫 슬라이드**(IG 그리드 커버) |

---

## 4. [요청 3 해결] `media_ids` 다건 조회 — 호출 1회

(B) 안으로 넣었습니다. Graph 의 batch-by-ids 문법을 써서 **id 개수와 무관하게 호출 1회**입니다.

```
GET /api/v1/integrations/instagram/workspaces/{ws}/media/?media_ids=1790…,1791…
```

- 최대 **50개**. `limit`/`after` 는 무시됩니다.
- 응답 `data` 는 **요청한 순서**를 따릅니다.
- 접근 불가/삭제된 id 는 `data` 에서 빠지고, 어떤 id 가 빠졌는지는
  **`query.missing_media_ids`** 로 알려줍니다.
- `paging` 은 빈 객체입니다(커서 없음).
- **200개보다 오래된 게시물도 바로 찾힙니다** — "고를 수는 있는데 상세에선 못 찾는" 문제 해소.

> 이 엔드포인트가 주는 `media_url`/`thumbnail_url` 은 **IG CDN 의 만료되는 URL** 입니다.
> 저장하지 마세요. 캠페인 카드/상세 썸네일은 캠페인 응답의 `thumbnail_url` 을 쓰세요.

> 단건 조회(A안)도 `GET .../workspaces/{ws}/media/{media_id}/` 로 이미 존재하지만,
> 댓글까지 함께 긁어오는 **개발용** 엔드포인트라 썸네일 용도로는 권하지 않습니다.

---

## 5. [요청 4 해결] 자리표시자 행 — 운영 2건, 정리했습니다

사과하실 일 아닙니다. 다만 조회 결과를 정확히 공유드리면:

```sql
SELECT count(*) FROM auto_dm_campaigns
 WHERE media_url LIKE '%images.unsplash.com/photo-1611162617474%';
-- 2건
```

| 캠페인 | IG 계정 | 상태 |
|---|---|---|
| 편평사마귀 제거 정보 제공 캠페인 | `yeonhada__` | active |
| 여드름 피부 리셋 꿀팁 공유 캠페인 | `yeonhada__` | active |

- 이 2건은 `media_url = ''` 로 정리했고, permalink 백필이 실제 게시물 링크를 채웠습니다.
- **정정 하나**: 이 값이 "서버 보강까지 막는다"는 부분은 이제 해당되지 않습니다.
  썸네일이 별 컬럼이 되면서 `media_url` 값과 무관해졌습니다. 이 행들이 막고 있던 것은
  **게시물 permalink 백필**(어드민·"게시물 보기" 링크)이었습니다.
- 서버도 이제 방어합니다: 저장 시 이 URL 패턴은 permalink 로 취급되지 않고,
  `media_url` 은 어차피 이미지로 쓰이지 않습니다.

---

## 6. [요청 5 결정] `media_url` 필드 정책 — (A)+(B) 둘 다

- **(A)** `max_length` **200 → 512** 로 넓혔습니다. 긴 URL 을 넣어 400 이 나던 footgun 제거.
  (실제 permalink 는 40~60자라 여유는 충분합니다.)
- **(B)** 문서에 못 박았습니다. 스키마 `description` 과 필드 `help_text` 가 이제 이렇게 말합니다:
  > 게시물 permalink. **이미지가 아니다 — 썸네일 표시에는 `thumbnail_url` 을 쓸 것.**

즉 `media_url` 은 **"게시물 링크"**, `thumbnail_url` 은 **"게시물 이미지"** 로 의미를 완전히
분리했습니다. 프론트는 문서대로 `thumbnail_url` 만 이미지로 쓰시면 됩니다.

---

## 7. [요청 6 해결] 미디어 목록 엔드포인트 문서화

- 정본 경로는 **`GET /api/v1/integrations/instagram/workspaces/{workspace_id}/media/`** 입니다.
  캠페인 생성 문서에 적혀 있던 `.../instagram/{workspace_id}/media/list/` 는 **존재하지 않는
  경로**였습니다(문서 오기) — 해당 서술을 정정했습니다.
- 스펙에서 검색되지 않던 이유는 태그가 `개발 전용` 이었기 때문입니다. **`Integrations` 태그로
  옮기고** `limit` / `after` / `ig_connection_id` / `media_ids` 파라미터와
  `paging.cursors.after` 응답 구조를 모두 명시했습니다. Swagger 에서 바로 보입니다.

---

## 8. 회신 체크리스트

- [x] **2번** — 운영 실제 캠페인의 값·길이: `media_url` **최대 72자**(잘린 적 없음),
      68/77 건이 **permalink**. 즉 "한도 때문"이 아니라 **"미러 대상이 permalink 였기 때문"**.
      메모리에만 실어 보내던 보강은 릴스·캐러셀에서 실패. → 근본 재설계로 해결.
- [x] **1번** — 상세 응답에 `thumbnail_url` + 통계 4필드 추가. 이번 배포 포함.
- [x] **4번** — 자리표시자 **2건**, 정리 완료(permalink 재백필됨).
- [x] **3번** — `media_ids` 다건 필터 추가(호출 1회, 순서 보존, 누락 id 보고).
- [x] **5·6번** — `media_url` 512자 + 의미 문서화 / 미디어 엔드포인트 스펙 등록.

## 9. 운영 반영 결과 (2026-08-05 배포 완료)

배포 후 기존 캠페인 전체를 일괄 백필했습니다.

| 구분 | 건수 | 비고 |
|---|---|---|
| **썸네일 확보** | **70** | 이미지·릴스·캐러셀 전부. 70건 모두 URL 응답 200/`image/jpeg` 확인, 평균 40KB(11~73KB) |
| 게시물이 삭제됨 | 3 | Graph `code=100 subcode=33`(객체 없음) — 구조적으로 불가. `thumbnail_url: null` |
| IG 연결 해지(revoked) | 1 | 토큰이 없어 조회 불가. 재연동 시 스위퍼가 자동으로 채웁니다 |
| 게시물 미지정(`any_media`) | 3 | 게시물 개념이 없어 해당 없음 |
| 합계 | 77 | |

- 응답 실측: 캠페인 16개 워크스페이스 목록 조회 **0.06~0.11초**(예전엔 항목당 Graph 동기
  호출 최대 5초 × 16 이 될 수 있었습니다).
- `thumbnail_url` 에 permalink 나 IG CDN URL 이 섞인 행: **0건**.
- 자리표시자 2건은 정리 후 실제 게시물 permalink 가 채워졌습니다
  (`.../reel/DWL0BHmkku7/`, `.../reel/DWB3EUnEmMk/`).

> 참고: 위 "게시물이 삭제됨" 3건은 **캠페인은 active 인데 대상 게시물이 사라진 상태**입니다.
> 썸네일과 무관하게 트리거 자체가 불가능하니, 화면에서 정리 안내를 노출할 가치가 있습니다.
> (필요하면 `thumbnail_sync_error` 를 노출하는 API 를 추가로 열어드릴 수 있습니다.)

## 10. 프론트 확인 포인트

1. 캠페인 목록·상세에서 카드 이미지가 뜨는지
2. 릴스·캐러셀 캠페인도 이미지가 뜨는지 (예전엔 구조적으로 불가)
3. 새 캠페인 생성 직후 → 몇 초 뒤 재조회 시 썸네일이 채워지는지
4. `media_url` 을 이미지로 쓰는 코드가 남아 있지 않은지 (있으면 계속 깨집니다)
