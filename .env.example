# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
DJANGO_SETTINGS_MODULE=config.settings.local

# Database (PostgreSQL)
DB_NAME=instagram_service
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Application
WEB_PORT=8000
ALLOWED_HOSTS=localhost,127.0.0.1

# Celery
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# Instagram API (추후 설정)
# INSTAGRAM_APP_ID=
# INSTAGRAM_APP_SECRET=
# INSTAGRAM_REDIRECT_URI=

# TikTok Content Posting API (developers.tiktok.com)
# 미감사 클라이언트는 모든 게시물이 PRIVATE 강제. 검수 후 PUBLIC 가능 (5–10영업일).
# TIKTOK_CLIENT_KEY=
# TIKTOK_CLIENT_SECRET=
# TIKTOK_REDIRECT_URI=
TIKTOK_MOCK_MODE=True

# TikTok Business API (business-api.tiktok.com) — Phase 2 (organic 댓글 hide/unhide)
# TIKTOK_BUSINESS_APP_ID=
# TIKTOK_BUSINESS_APP_SECRET=
# TIKTOK_BUSINESS_REDIRECT_URI=

# YouTube / Google OAuth (Data API v3)
# 기본 일일 quota = 10,000 units. videos.insert = 1,600 units/회.
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=
# GOOGLE_OAUTH_REDIRECT_URI=
YOUTUBE_MOCK_MODE=True
# YOUTUBE_DAILY_QUOTA=10000

# Security (Production)
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True

# CORS (개발용)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# Logging
LOG_LEVEL=INFO
