# Environment files
.env
.env.local
.env.production

# Backup secrets & artifacts (GATE-0) — *.example 템플릿은 추적 유지(이름이 달라 매칭 안 됨)
.env.backup
.env.bak*
.deploy.prev

# 이메일 템플릿 미리보기 산출물 (scripts/preview_emails.py) — 커밋 불필요
/email_previews/
/*_preview.png

# JWT RS256 키 (개인키 포함) — 절대 커밋 금지. 공개키만 외부 전달.
/secrets/
*.pem
/backups/
*.dump.gpg
*.sql.gz.gpg
deploy/backups/pgbackrest.conf

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
/staticfiles/
/media/

# Testing
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.tox/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Docker
*.pid
*.seed
*.pid.lock

# Jupyter Notebook
.ipynb_checkpoints

# Celery
celerybeat-schedule
celerybeat.pid

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

test/
# GeoIP DB
geoip/
*.mmdb
nul

# Debug/experiment artifacts (AI 페이지 생성 실험 스크린샷, Playwright MCP 스냅샷)
# 루트의 디버그 PNG(baseline-*, exp-*, kim-seg*, gb-* 등)와 playwright 페이지 스냅샷.
/*.png
/.playwright-mcp/

# DM 캠페인 이전 실데이터 실험 산출물(외부 유저 IG 데이터 — 커밋 금지)
/_miniai_raw.json
/DM_MIGRATION_EXPERIMENT_*.html
