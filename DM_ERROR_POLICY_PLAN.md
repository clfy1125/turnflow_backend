# DM 오류 처리방침 — 어드민·백엔드 통일 및 패치 계획

작성 2026-07-30 · 기준 브랜치 `feat/toss-billing` · 분류 원본 `DM_ERROR_POLICY_MATRIX.html`

---

## 0. 요약

1. **분류 51건 확정** — 🔴17 / 🟠4 / 🔵2 / 🟣2 / ⚪26 (기본값에서 13건 변경).
2. **어드민 사이트에 실제 버그 3건 + 문구 드리프트 3건**을 찾았습니다. 그중 하나(딥링크 `failed_no_trace → hidden_spam`)는 지금도 "링크를 눌렀는데 목록이 비는" 증상을 냅니다.
3. **"24시간 창" 판단은 코드와 반대로 걸려 있습니다.** 우리 지연이 확정인 행은 ⚪로, 우리 지연과 무관할 수 있는 행은 🔴로 정해져 있습니다 (§3). 여기만 다시 정하면 나머지는 그대로 진행 가능합니다.
4. 통일 방법은 **서버 사전에 `policy` 필드 하나를 추가**하는 것입니다. 상태값(status)·집계 정의는 건드리지 않으므로 **마이그레이션 0건, prod 무중단**입니다.

---

## 1. 확정된 분류

| 분류 | 건수 | 어떤 것들인가 |
|---|---:|---|
| 🔴 확인해야함 | 17 | 24시간 창 계열 4, 세부번호 없는 code 10·100·200, `-1`, legacy 2, 미분류 4xx·단서0·사전미등록, 건너뜀 기타, 2534001 |
| 🟠 사용자 조치 | 4 | 190 · 102 · pre-send `failed_token` · 숨겨진 요청(2534025) |
| 🔵 관리자 조치 | 2 | 2534023(이미 답글) · `failed_no_trace`(도착 미확인) |
| 🟣 수신자 사정 | 2 | 2534014 · 551 |
| ⚪ 정책상 정상 | 26 | 레이트리밋 계열, 복구 퍼널, 건너뜀 8종 대부분, 정상 상태 9 |

### 기본값에서 바꾼 13건 (= 방침이 실제로 바뀐 지점)

| 항목 | 제안 → 결정 | 근거(메모 요약) |
|---|---|---|
| `100/2534022`, `10/2534022`, `10/2018278`, `10+failed_window` | ⚪ → 🔴 | "우리 서버가 24시간 응답 못 한 것이므로 심각" — **§3에서 재검토 필요** |
| `200/2534066` | 🔵 → 🔴 | 대응 정책 사내 논의 중 (게시물 문제 고지 + 대체 발송 방식 연구) |
| `100`(세부번호 없음) | ⚪ → 🔴 | 7일 초과는 우리가 미리 알 수 있으니 제외하고, 그 외 100번은 조사 |
| `failed_param`(코드 없음) | ⚪ → 🔴 | 7일 초과면 정상, 그 외면 조사 |
| `no_trace` 센티넬 | 🔵 → ⚪ | 디버깅용으로 남기고 "안 쓰임" 명시만 |
| 월 DM 한도 | 🟠 → ⚪ | 정상 동작이며 프론트에서 결제 유도 |
| IG 계정 비활성 | 🟠 → ⚪ | 정상 동작 |
| 중복 캠페인 정리 · 유령 오프닝 정리 | 🔵 → ⚪ | 이미 코드 개편으로 재발 방지됨 |
| `2534001` | 🟣 → 🔴 | 어드민 화면 기준으로 쓴 초안이라 실측 확인 필요 |

---

## 2. 조사 ① — 어드민 사이트가 백엔드와 다르게 처리하는 지점

어드민(`10_turnflow_admin`)은 **오류 상세 팝업·운영 대시보드는 서버 사전을 제대로 쓰고 있습니다.** 문제는 그 주변의 로컬 사전과 딥링크 매핑입니다.

### 2-1. 버그 (지금 실제로 잘못 동작)

| # | 위치 | 내용 | 증상 |
|---|---|---|---|
| B1 | `src/app/(dashboard)/auto-dm/logs/page.tsx:71` | `SEND_STATUS_TO_GROUP` 이 `failed_no_trace → "hidden_spam"` 으로 매핑. 백엔드는 `failed_no_trace → attention` (`dm_status_groups.py:74`) | 운영 대시보드 "도착 미확인 N건" 링크를 누르면 `status_group=hidden_spam` 으로 필터돼 **해당 건이 하나도 안 보임** |
| B2 | 같은 파일 `:81` | `SEND_STATUS_TO_CODE` 가 딥링크 시 오류 코드 2차 필터를 자동 프리셋 (`failed_token→190`, `failed_window→10`, `failed_param→100`) | code 가 비어 있거나 다른 값인 행(102, 100+2534022, 코드 없는 내부 종결)이 **필터에서 잘려 나감**. "대시보드는 12건인데 목록은 5건" 류의 불일치 |
| B3 | `src/lib/status.ts:113` | `dmErrorCode` 맵(코드 키)에 **subcode** `2534025` 가 섞여 있음 | code=2534025 인 로그는 없으므로 이 항목은 영원히 안 잡힘(죽은 항목). 반대로 code 200·551·-1·102 는 맵에 없어 폴백 라벨로 표시 |

### 2-2. 문구 드리프트 (틀리진 않지만 어드민·유저·백엔드가 서로 다른 말)

| # | 항목 | 백엔드 | 어드민 |
|---|---|---|---|
| D1 | 상태 그룹 라벨 (`dm_status_groups.GROUP_DISPLAY`) | 대기중 / 전송됨 / 읽음 / 숨겨진 요청·스팸 / **확인 필요** | 대기 / 발송됨 / 읽음 / **스팸함 유입** / **오류** (`status.ts:67`) |
| D2 | `failed_window` 라벨 | "24h 윈도우 만료" | "24h 창 만료" — **둘 다 부정확**. 댓글 경로 실제 창은 7일 (§3) |
| D3 | 목 사전 `src/mocks/dmErrorCatalog.ts` | 서버 사전 전수 | `100/2534023`·`551`·`4`·`no_trace`·legacy 2종 **누락**, `613` 을 `recoverable:true` 로 잘못 표기(서버는 false) |

> 목 사전은 MSW 목 응답 전용이라 prod 화면에는 영향이 없지만, **화면 검증이 실제와 다른 값으로 통과**하므로 회귀를 놓칩니다.

### 2-3. 잘 되어 있는 것 (건드리지 말 것)

- 오류 상세 팝업(`ErrorBlock`)은 서버 `error_title/cause/action/recoverable` 이 1순위, 로컬 사전은 폴백뿐 — 설계대로입니다.
- 운영 대시보드 오류 상세는 서버가 준 `group`(failed / hidden_spam)으로 2섹션 분리 + KPI 합계 대조 경고까지 있습니다.
- `skipped_breakdown` 의 `actionable` 도 서버 판정을 그대로 씁니다.

---

## 3. 조사 ② — "24시간 창" 사실 정정 ★ 결정 재검토 필요

메모에 적으신 근거는 **"우리 서버가 24시간 동안 응답 못 한 것"** 입니다. 코드를 보면 그 근거가 성립하는 행과 성립하지 않는 행이 **정확히 반대로** 분류돼 있습니다.

### 3-1. 창의 길이는 경로에 따라 다릅니다 — 24시간이 아닐 수 있습니다

```python
# apps/integrations/models.py:1647
return timedelta(days=7) if self.comment_id else timedelta(hours=24)
```

- **댓글 트리거 오프닝 DM**(비공개 답장, `comment_id` 사용) → 창은 **7일**
- **user_id 경로**(팔로우게이트 리워드, 스토리 답장, 복구 재발송) → 창은 **24시간**

즉 지금 어드민·유저 화면에 뜨는 "24h 창 만료"는 오프닝 DM 에 대해서는 **틀린 문구**입니다.

### 3-2. 우리가 늦은 게 확정인 것은 ③번 행입니다

```python
# apps/integrations/tasks.py:1554-1565  (내부 가드)
age = timezone.now() - log.created_at
if age >= _messaging_window(log):
    log.mark_failed(status=FAILED_WINDOW,
                    error_message="Messaging window expired while waiting for send capacity")
```

- 이 가드는 **로그 생성 시각부터** 잽니다. 발동했다는 건 **우리 큐에서 7일(또는 24시간) 이상 묵혔다**는 뜻이고, 원인은 100% 우리 쪽입니다.
- 이 건은 `error_code` 가 **비어 있고** 메시지가 `Messaging window expired while waiting for send capacity` 입니다 → HTML 의 ③번 "줄 서서 기다리는 사이 24시간이 끝남" 행. **여기가 ⚪로 결정돼 있습니다.**

### 3-3. Meta 가 준 2534022 는 우리 지연을 뜻하지 않을 수 있습니다

Meta 의 창 기준점은 **수신자의 마지막 상호작용 시각**이지 우리 로그 생성 시각이 아닙니다. 우리가 즉시 보냈어도 거부될 수 있는 경우:

- 웹훅이 늦게 도착했거나(구독 auto-disable 후 재구독 등) 보정 폴러가 오래된 상호작용을 주운 경우
- 어드민에서 오래된 건을 **수동 재발송**한 경우
- 스토리 답장처럼 상호작용 시점과 발송 시점이 벌어지는 경로

이 조합들(`100/2534022`, `10/2534022`, `10/2018278`)이 지금 🔴로 결정돼 있습니다.

### 3-4. 확정 기준 (2026-07-30 결정)

> **"서버가 고장 나서 창이 끝난 것 = 관리자 확인(🔴). 순간 피크로 밀려서 끝난 것 = 정상, 유저에게 알리기만(⚪)."**

이 기준은 코드에서 **기계적으로 판정 가능**합니다. 근거는 발송 태스크가 상태를 남기는 방식입니다.

| 상황 | `retry_count` | `next_retry_at` | 판정 |
|---|---|---|---|
| 페이서 슬롯 대기 · 시간당 백스톱 · Action Block 쿨다운 | 안 오름 | **계속 미래로 갱신** (`tasks.py:1591`) | 피크 ⚪ |
| Meta 일시 오류 재시도 | +1 (상한 24) | 갱신 (`tasks.py:345`) | 피크 ⚪ |
| **워커 다운 · 태스크 유실 · requeue 워커 정지** | 멈춤 | **과거 시각에 멈춘 채 방치** | 방치 🔴 |

→ 판정식: **`next_retry_at`(없으면 `updated_at`)이 한참 지났는데 아직 발송되지 않았다 = 서버 방치.**
임계는 기존 `DM_BACKLOG_OLDEST_ALERT_HOURS`(2h)와 정렬합니다.

### 3-5. Meta 가 준 2534022 는 "우리 방치"가 구조적으로 아닙니다 — 그래도 🔴 유지가 맞습니다

내부 가드는 **`send_dm_task` 진입부**에 있습니다(`tasks.py:1554`). 창이 지난 건은 **Meta 를 부르기 전에** 종결되므로, Meta 가 2534022 를 반환했다는 것은 **우리 기준으로는 창 안이었다**는 뜻입니다. 즉 우리가 24시간 방치한 건이 Meta 2534022 로 나타날 수는 없습니다.

그럼 왜 거부됐나 — **기준점이 다르기 때문**입니다. 우리는 `log.created_at`(웹훅 수신 시각)부터, Meta 는 **수신자의 마지막 상호작용**부터 잽니다. 이 둘이 벌어지는 경우:

- 웹훅이 늦게 도착 (구독 auto-disable 후 재구독, Meta 재전송 지연) ← **우리/Meta 문제일 수 있음**
- 보정 폴러가 오래된 상호작용을 주움
- 어드민에서 오래된 건을 수동 재발송

**세 번째까지는 정상이지만 첫 번째는 우리 문제**이고, 코드만으로는 어느 쪽인지 확정할 수 없습니다.
→ "확정적으로 알 수 없으면서 확인이 필요한 것"에 해당하므로 **🔴 확인해야함 유지**가 맞습니다(결정 그대로).

### 3-6. 결론 — 이 4행만 이렇게 정리됩니다

| 행 | 최종 |
|---|---|
| ③ `failed_window` 내부 가드 · **방치 판정** | 🔴 관리자 확인 (신규 분리) |
| ③ `failed_window` 내부 가드 · **피크 판정** | ⚪ 정상 (유저에게 "요청이 몰려 못 보냈다" 안내) |
| ① `100/2534022`, `10/2534022`, `10/2018278` | 🔴 유지 (트리거~발송 갭 원인 확인 필요) |
| `10 + failed_window` (세부번호 없음) | 🔴 유지 |

---

## 4. 통일 설계 — 서버 사전에 `policy` 필드 하나

정본은 **`apps/admin_api/dm_error_catalog.py`** 하나로 유지하고, 결정한 분류를 그 사전에 실어 내려줍니다.

```python
# 각 엔트리에 policy 추가 (값 5종)
#   investigate | user_action | admin_action | recipient | normal
{
  "title": "...", "cause": "...", "action": "...",
  "policy": "investigate",
  "policy_display": "확인해야함",
}
```

내려가는 곳 (전부 **필드 추가만**, 기존 필드 불변):

| 엔드포인트 | 추가 필드 |
|---|---|
| `GET /admin/dashboard/ops/` → `dm_quality.failure_breakdown[]` | `policy`, `policy_display` |
| `GET /admin/auto-dm/logs/{id}/` (`describe_for_log`) | `error_policy`, `error_policy_display` |
| `GET /admin/auto-dm/recipients/` | `error_policy` (대표 오류 기준) |
| 유저 콘솔 `dm_frontend_actions.build_frontend_action` | `severity` 를 `policy` 와 정합하게 정리 (§6) |

이렇게 하면:
- 어드민은 **색·정렬·필터를 서버 값으로만** 그립니다 → 로컬 사전을 두 번 고칠 일이 없어집니다.
- 새 오류 코드가 등장해도 **백엔드 배포 한 번**으로 방침이 반영됩니다.
- 건너뜀(skipped)의 `actionable` 도 같은 어휘(`policy`)로 통일합니다.

---

## 5. 패치 계획 (prod 가동 중 안전 순서)

### 지켜야 할 3원칙

1. **`SentDMLog.status` 값은 추가·변경 금지.** 집계·큐·페이서·통계가 전부 이 값에 묶여 있습니다. 이번 작업은 전부 "설명·분류" 레이어에서만 합니다 → **마이그레이션 0건**.
2. **캐시는 선별 삭제.** 응답 모양이 바뀌면 `admin:dash:ops:*` (+ `admin:dash:mkt:*`) 만 지웁니다. **전체 flush 금지** — `rate_governor` 센티넬이 날아가면 DM 이 1시간 멈춥니다.
3. **집계 정의(실패율·KPI) 변경 금지.** 이번 패치는 숫자를 바꾸지 않고 **분류 라벨만** 붙입니다.

### Phase 0 — 실측 (배포 없음, 최우선)

```bash
# prod
docker compose exec web python manage.py dump_dm_error_census --format csv > census.csv
```

확인할 것:
- `2534001` 이 실제로 존재하는가 → §1 의 🔴 결정을 확정하거나 항목 자체를 폐기
- `failed_window` 중 **코드 없음 + `Messaging window expired while waiting%`** 비율 → §3 재결정의 근거
- `code=10 subcode 없음` 의 원문 패턴 → 권한/윈도우 자동 분기 규칙을 만들 수 있는지
- `catalog=MISSING` 줄 → 사전 보강 목록

> 이 단계 없이 색부터 칠하면 "빨간 게 300건인데 볼 수 없는" 대시보드가 됩니다.

### Phase 1 — 백엔드: `policy` 필드 추가 (무해, 되돌리기 쉬움)

- `dm_error_catalog.py` 에 `policy` 추가 + `_POLICY_BY_STATUS` 기본값
- `describe()` / `describe_for_log()` / `failure_breakdown` 에 노출
- 테스트: 51개 조합 전수 `policy` 단언 (`tests_dm_error_policy.py` 신규)
- 배포 후 `admin:dash:ops:*` 선별 삭제
- **어드민 배포 전이어도 안전** — 프론트는 모르는 필드를 무시합니다(zod `.catch()` 관용 스키마)

### Phase 2 — 백엔드: `failed_window` 를 피크 / 방치로 분리 (§3-4 구현)

내부 가드가 종결하는 순간에 판정해서 **`error_subcode` 에 표식만** 남깁니다.
내부 가드분은 원래 subcode 가 비어 있으므로 **Meta subcode 와 충돌하지 않고, 마이그레이션도 불필요**합니다.

```python
# apps/integrations/tasks.py — 내부 가드(1554~1565)
STALL_HOURS = getattr(settings, "DM_BACKLOG_OLDEST_ALERT_HOURS", 2)

def _window_expiry_kind(log) -> str:
    """창 만료가 우리 방치인지(피크 적체인지) 판정.

    페이서·백오프는 매번 next_retry_at 을 미래로 갱신한다(tasks.py:1591, 345).
    그 시각이 한참 지났는데도 아직 큐에 남아 있다 = 아무도 안 집어갔다 = 서버 문제.
    """
    ref = log.next_retry_at or log.updated_at
    return "stalled" if timezone.now() - ref > timedelta(hours=STALL_HOURS) else "peak"

log.mark_failed(
    status=SentDMLog.Status.FAILED_WINDOW,
    error_message=(f"Messaging window expired while waiting for send capacity "
                   f"(window={'7d(comment)' if log.comment_id else '24h(user_id)'}, "
                   f"retry_count={log.retry_count})"),
    error_subcode=f"window_{_window_expiry_kind(log)}",   # window_stalled | window_peak
)
```

- 사전에 두 줄 추가: `("", "window_stalled")` → 🔴, `("", "window_peak")` → ⚪
- 문구 정정: "24시간 창" → **"댓글 답장 7일 / DM 24시간"** (경로별)
- 과거 데이터는 빈 subcode 그대로 → 기존 폴백 문구로 표시, **소급 변경 없음**
- `window_stalled` 는 1건이라도 나오면 텔레그램 경고 (Phase 5)

### Phase 3 — 백엔드: 사전 공백 메우기 (Phase 0 결과 반영)

- `census` 의 MISSING 조합 등록 (`1/2/17/32/368`, 필요 시 `2534001`)
- 죽은 항목 정리: `_BY_CODE["no_trace"]` 는 삭제 대신 **"미사용(디버깅용)" 주석 명시** — 결정대로

### Phase 4 — 어드민 프론트

1. **B1 수정**: `SEND_STATUS_TO_GROUP.failed_no_trace` → `"attention"`
2. **B2 수정**: `SEND_STATUS_TO_CODE` 제거(또는 코드 프리셋 없이 그룹만 전달)
3. **B3 수정**: `dmErrorCode` 에서 `2534025` 제거, `102/200/551/-1` 추가 — 다만 서버 `policy`/`error_title` 이 1순위이므로 이 맵은 **폴백 최소화**가 방향
4. **D1 통일**: 그룹 라벨을 서버 `status_group_display` 로 렌더 (로컬 라벨 맵 제거)
5. **D3 동기화**: `mocks/dmErrorCatalog.ts` 를 서버와 1:1 (누락 6줄 + `613 recoverable` 오기)
6. `policy` 기반 색·정렬·필터 추가: 목록/대시보드에서 **"확인해야함"만 보기** 필터

### Phase 5 — 모니터링

- `dashboard_constants.py` 에 `policy=investigate` 건수 임계값 추가 → 운영 대시보드 "처리 필요"에 노출
- 특히 Phase 2 의 `internal_window_expired` 는 **1건이라도 나오면 경고** (우리 적체 신호)

### 롤백

Phase 1·3·4 는 코드 되돌리면 즉시 원복(데이터 오염 없음).
Phase 2 의 `error_subcode` 표식은 데이터에 남지만 무해하며, 사전에서 해당 줄만 빼면 기존 문구로 돌아갑니다.

---

## 6. 유저 콘솔(프론트) 반영 사항

> 원칙: 유저에게 `investigate` / `admin_action` 을 그대로 보여주지 않습니다. 둘 다 **"확인 중입니다"** 로 접고, 유저가 실제로 할 수 있는 것만 CTA 로 띄웁니다.

| # | 화면 | 지금 | 바꿀 것 | 근거 |
|---|---|---|---|---|
| F1 | 로그 상세 (2534014) | "댓글이 7일 초과됐거나 ID가 유효하지 않습니다" 로 뭉뚱그림 | **"수신자를 찾을 수 없음"** 으로 분기. `build_frontend_action` 의 `failed_param` 분기가 지금 `2534025` 만 갈라냄 | 메모: "수신자를 찾을수없음 이라고 떠야됨" (`dm_frontend_actions.py:136`) |
| F2 | 연동 상태 배너 | 로그 상세에만 재연동 CTA | 190/102 발생 시 **대시보드 상단 배너**로 재연동 안내 (연결 status 기반) | 메모: "재연동하라고 프론트에서 알려줘야함" |
| F3 | 건너뜀 문구 | "발송 제한(**시간당 한도**) 또는 정책에 의해" | **시간당 한도는 v4.3 에서 제거된 개념** → "이번 달 발송 한도 소진" + **업그레이드 CTA** | `dm_frontend_actions.py:198`, 마이그 0042 |
| F4 | 24시간 만료 문구 | "24시간 경과" 단일 문구 | 경로별로 "댓글 답장은 7일 / DM 은 24시간" | §3-1 |
| F5 | 발송 적체 안내 | 없음 | 내부 가드로 만료된 건은 **"요청이 몰려 제때 보내지 못했습니다"** 로 솔직히 안내 | 메모: "DM 요청이 순간적으로 많이 와서 못보냈다고 알려주기만 하면 될 것" |
| F6 | 게시물 차단(2534066) | 일반 실패로 보임 | **"이 게시물은 인스타가 자동 DM 을 막았습니다"** + 새 게시물 안내 (사내 정책 확정 후) | 메모: 대응 정책 논의 중 |
| F7 | 상태 라벨 | 콘솔·어드민 용어 상이 | `status_group_display` 서버 값으로 통일 | D1 |

---

## 7. 패치 후 어드민 "발송 안 됨" 화면은 이렇게 나뉩니다

HTML 의 **51개는 "사전 항목 수"**(= 설명 문구 개수)이지 화면 분류가 아닙니다.
화면은 **3단**으로 접힙니다.

```
[1단] 요약 칩 5개   🔴 확인해야함 12 · 🟠 사용자 27 · 🔵 관리자 4 · 🟣 수신자 9 · ⚪ 정상 143
   ↓ 클릭
[2단] 사유 카드 16개 (건수 0 이면 숨김 → 실제로는 보통 5~8개만 보임)
   ↓ 클릭
[3단] 코드별 원본 목록  ← 51개 사전 항목이 여기서 쓰임 (code/subcode/Meta 원문)
```

### 2단 사유 카드 — 총 16개

**A. 안 보냄 (건너뜀) — 3개**

| 색 | 카드 | 흡수하는 항목 | 화면 조치 |
|---|---|---|---|
| ⚪ | 월 한도 소진 | `monthly_dm_limit` | 결제 유도(유저) |
| ⚪ | 설정·정리로 건너뜀 | 캠페인 꺼짐 · 시간대 밖 · 계정 비활성 · 자기 댓글 · 연결 해제 · 중복 캠페인 정리 · 유령 오프닝 정리 (7종) | 없음 |
| 🔴 | 분류 안 된 건너뜀 | `other` | 원문 열기 → 사전 보강 |

**B. 실패 — 11개**

| 색 | 카드 | 흡수하는 항목 | 화면 조치 |
|---|---|---|---|
| 🟠 | 재연동 필요 | `190` · `102` · pre-send `failed_token` | 고객에게 재연동 안내 |
| 🔴 | **발송 방치 (우리 문제)** | `window_stalled` *(Phase 2 신규)* | 큐/워커 점검 · 텔레그램 경고 |
| ⚪ | **몰려서 지연됨 (피크)** | `window_peak` *(Phase 2 신규)* | 없음 (유저 안내만) |
| 🔴 | 창 만료 · 원인 확인 필요 | `100/2534022` · `10/2534022` · `10/2018278` · `10+failed_window` | 웹훅 지연 여부 확인 |
| ⚪ | 댓글 7일 초과 | code 100 중 **원문으로 7일 초과가 확정된 건** *(Phase 3 에서 분리)* | 없음 |
| 🔴 | 파라미터 오류 · 원인 미확정 | 위에서 안 갈린 `100` · `failed_param` | 원문 확인 |
| 🔵 | 이미 답글 있음 | `100/2534023` | 중복 캠페인·타사툴 점검 |
| 🔴 | 게시물 자동 DM 차단 | `200/2534066` | (정책 확정 후) 게시물 교체 안내 |
| 🟣 | 수신자 사정 | `2534014` · `551` (+ `2534001` 실측 확인 후) | 없음 |
| 🔵 | 도착 미확인 | `failed_no_trace` | **재검증** 버튼 |
| 🔴 | 원인 불명 | `-1` · code `10`/`200`(세부번호 없음) · 미분류 4xx · 단서 0 · 사전 미등록 · legacy `failed`/`failed_api` | 원문 확인 → 규칙 추가 |

**C. 숨겨진 요청 · 스팸 — 2개** (실패 아님, KPI 분리 유지)

| 색 | 카드 | 흡수하는 항목 |
|---|---|---|
| ⚪ | 숨김함 유입 · 복구 대기 | `100/2534025` · `recovery_pending` |
| ⚪ | 복구 만료 | `recovery_expired` |

### 왜 51 → 16 으로 줄어드나

- **폴백 계층이 화면에서는 한 카드**로 접힙니다. 예를 들어 `190`(코드) · `failed_token`(상태) · pre-send 차단은 사전에서는 3줄이지만 **운영자가 할 일은 "재연동 안내" 하나**라 카드도 하나입니다.
- **정상 상태 9개**(queued/accepted/delivered/read 등)는 "발송 안 됨"에 아예 안 들어옵니다.
- **건너뜀 7종**은 전부 "설정대로 동작"이라 한 카드로 묶고, 내역은 카드 안에서 봅니다.

---

## 8. 결정이 필요한 3가지

1. **§3 재검토** — 내부 가드 만료(우리 적체)를 🔴로 올릴지, 그리고 Meta 2534022 를 🔴로 유지할지. Phase 0 실측 후 정하는 것을 권합니다.
2. **2534023 중복 발송 점검 방법** — 메모대로 "우리가 두 번 보냈는지"를 보려면, 같은 `comment_id` 에 우리 로그가 2건 이상인지 판정해 어드민 상세에 **"중복 발송 의심"** 배지를 띄우는 방식을 제안합니다(집계 변경 없음).
3. **2534066 정책** — 사내 논의 결론이 나오면 policy 를 🔴에서 확정 값으로 바꿉니다. 함께 넣을 수 있는 것: 대시보드에 **게시물별 실패 집중도** 패널(어느 게시물이 막혔는지 한눈에).
